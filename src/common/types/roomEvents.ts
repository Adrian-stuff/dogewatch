export enum ReceivedRoomEvents {
  USER_JOINED = "userJoined",
  USER_LEAVE = "userLeave",
  GET_PLAYERDATA = "getPlayerData",
  SYNC_PLAYERDATA = "syncPlayerData",
}

export enum SendRoomEvents {
  JOIN_ROOM = "joinRoom",
  CREATE_ROOM = "createRoom",
  LEAVE_ROOM = "userLeave",
  GET_PLAYERDATA = "getPlayerData",
}
